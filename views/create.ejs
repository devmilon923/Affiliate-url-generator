<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="flex items-center justify-center min-h-screen bg-gray-100">
    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-sm">
      <h1 class="text-2xl font-bold text-center mb-6">Create Account</h1>
      <% if (locals.error) { %>
      <p class="mb-2 text-red-500"><%= locals.error %></p>
      <% } %>
      <form action="./create" method="post" class="space-y-4">
        <input
          type="text"
          placeholder="Your name"
          name="name"
          required
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
        <input
          type="email"
          placeholder="Your email"
          name="email"
          required
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
        <input
          type="number"
          placeholder="Your PIN"
          name="pin"
          required
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
        
        <select
          name="permission"
          id=""
          class="p-2 w-full border bg-gray-50 text-sm rounded-md outline-none"
        >
        <option class="text-sm" value="" disabled selected>Set your account tpye</option>

          <option class="text-sm" value="NORMAL">NORMAL</option>
          <option class="text-sm" value="ADMIN">ADMIN</option>
        </select>
        <button
          type="submit"
          class="w-full py-2 bg-blue-500 text-white font-semibold rounded-md hover:bg-blue-600 transition duration-200"
        >
          Create Now
        </button>
        <a href="/login" class="block text-center">Login an account</a>
      </form>
    </div>
  </body>
</html>
